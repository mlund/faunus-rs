# Cargo.lock is tracked: the workspace ships binaries, and the regression
# fixtures compare at 1e-8, so a dependency bump must be a reviewable commit
# rather than a silent re-resolve.
target/
*.md
*.csv
*.xtc
*.pdb
*.xyz
*.yaml
.DS_Store


# Editor swap files and per-run simulation output
*.swp
*.swo
*.dat
